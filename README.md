# chore-wheel-app

To run the integration tests and verify the RTM requirements:
1. Navigate to the `backend` folder.
2. Install test dependencies: `pip install pytest httpx`
3. Run tests: `pytest tests/test_auth.py`
